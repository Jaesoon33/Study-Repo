1. java.lang 패키지
	- Object (모든 클래스의 최상위 부모 클래스)
		- 실무에서 반드시 재정의
			- equals(): == 과 동일 --> 내용(값) 비교로 변경
			- hashCode()
			- toString: 의미 있는 정보 출력
		
		- new할 때 JVM이 하는 일
			- Heap 메모리에 공간 확보
			- 필드 초기화 
			- hashCode 값 자동 부여 (메모리 주소 기반)
				- ex) 사람이 태어나면 --> 주민번호가 발금
					  객체가 new되면 --> hashCode가 자동 부여됨
				- 객체마다 다 hashCode가 달
				
		- 비교
		
			구분		hashCode()기준  m1 vs mwkrm 
			----------------------------------------
			재정의X	
			재정의O